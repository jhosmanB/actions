name: deploy to server

on: push
  
jobs:
  first_jobs:
    runs-on: ubuntu-latest
    env:
      NAME: var
    steps:
      - name: Hola Mundo
        run : ls -al

      - name: Action
        uses: actions/checkout@v4

      - name: despues del action
        run : ls -al
        
  second_jobs:
    needs: first_jobs
    runs-on: ubuntu-latest
    steps:
      - name: Hola Mundo
        run : echo "Hola Mundo 2 ${{github.ref}}"